import React, { ReactElement } from 'react';

interface Props {}

function Footer({}: Props): ReactElement {
	return <footer className='border-t-2 border-blue-700'>FOOTER</footer>;
}

export default Footer;
